import { Text } from "react-native";

export default function Page() {
  return (
    <Text className="text-foreground">
      This application will not support the web platform, it'll only do so for IOS
      and Android
    </Text>
  )
}